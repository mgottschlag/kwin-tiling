
add_subdirectory( dockbar ) 
MESSAGE(STATUS "taskbar panel extension disable (since taskbar lib is disabled), reactivate it when we use plasma or remove it")
#add_subdirectory( taskbar ) 
add_subdirectory( sidebar ) 
